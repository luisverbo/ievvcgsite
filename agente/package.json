{
  "name": "paginapro-agente",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Agente local de prospeccao: roda no seu computador, com seu IP.",
  "scripts": {
    "prospectar": "node --env-file-if-exists=.env --import tsx prospectar.ts",
    "instalar-navegador": "playwright install chromium",
    "servico": "node --env-file-if-exists=.env --import tsx servico.ts"
  },
  "dependencies": {
    "playwright": "^1.50.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5"
  }
}
